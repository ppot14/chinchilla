package com.chinchilla.persistence;import java.util.ArrayList;public interface IParentDAO<T, PK> {    public T get(PK id);//get obj of type T by the primary key 'id'     public T getByName(String name);//get obj of type T by the 'name' field, if one exists for that table    public ArrayList<T> getAll();//get all objects of type T    public int create(T objInstance);//insert an object of type T into the database    int update(T transientObject); //update an object of type T        int delete(PK id);//delete an object of type T}